export default function Hero() {
    return (
        <section className="hero">
            <h1>Essential tools for your documents and files</h1>
            <p>Merge, split, and convert PDFs instantly. Plus, discover resources with our open directory search.</p>
        </section>
    );
}
